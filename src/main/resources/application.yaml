spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/easydict?user=postgres&password=slava

  liquibase:
    change-log: classpath:db/changelog/changelog.xml
    enabled: true

